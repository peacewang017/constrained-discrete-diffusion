name: small
type: ddit
hidden_size: 768
cond_dim: 128
length: ${model.length}  # Same length as diffusion model
n_blocks: 12
n_heads: 12
scale_by_sigma: True
dropout: 0.1
num_classes: ${data.num_classes}
pooling: mean
